import { ImageGenerator } from '@/components/ImageGenerator';

export const metadata = {
  title: 'Generate - AI Image Generator',
  description: 'Create stunning AI-generated images with advanced FLUX 1.1 Pro model. Transform your ideas into beautiful artwork and designs.',
};

export default function GeneratePage() {
  return <ImageGenerator />;
}